You are converting a verified optimization instance into a concise natural-language business problem.

Return exactly one JSON object with these keys:
- `problem_background`: one short sentence naming the organization, trigger, and decision owner.
- `problem_statement`: a self-contained English problem statement.
- `structured_problem_data`: compact structured entities, sets, parameters, and units that appear in the statement.

Compact-output rules:
- Keep `problem_statement` between 900 and 2600 characters whenever possible, and never exceed 3400 characters.
- Use one business paragraph plus compact bullet lists or compact tables. Do not write a long narrative.
- Preserve all essential numeric data needed to reconstruct the optimization model. For large indexed data, use dense lists/tables and avoid prose repetition.
- If `source_artifacts.compact_source_data.available` is true, treat it as the primary numeric source and include its complete tables whenever possible.
- If a full table is unavailable because an artifact preview is truncated, do not invent omitted values. State the available dimensions, ranges/previews, and explicitly available values only.
- Do not reveal the optimal objective value, solver status, variable solution, mathematical formulation, LP/MPS text, Python code, or Gurobi code.
- Do not include solver-validation language or training-data commentary.

Business framing rules:
- Use `scenario_guidance.selected_scenario`, `scenario_guidance.scenario_variant`, and `scenario_guidance.selected_business_trigger` as the business wrapper.
- Put the organization, trigger, decision owner, and operating context inside `problem_statement` itself because downstream stages consume that field directly.
- Keep the scenario realistic but focused. Avoid template openings such as "A company has..." unless the scenario gives no better stakeholder.
- Do not introduce incompatible structures. For example, do not turn a network-flow or transportation instance into a vehicle-routing story unless routes, vehicles, time windows, and sequencing variables are present.

Mathematical-faithfulness rules:
- Use `generator_profile.canonical_math_signature` as a hard signature. Preserve objective sense, variable types, required tables, and core constraint families.
- Use `generator_profile.concept_tags` only to clarify the source structure. Do not add unsupported constraints, resources, periods, arcs, costs, or eligibility rules.
- If the instance is large, prefer concise indexed notation in natural language, such as "for each listed arc (i,j)" or "for each product-period pair shown below", but include concrete numeric values when available.
- The result must be suitable as the prompt side of a CPT training document.

Generator-specific compacting rules:
- For `optmath_facility_location`, preserve the three decision layers exactly: binary `Selected[d]`, binary `Served[d,z]`, and continuous `Shipped[c,p,d,z]`. Include supply, demand, distribution-center fixed/unit throughput costs, throughput bounds, and the four-index shipping-cost table. Do not describe vehicles, tours, routes, or customer visit sequencing.
- For `optmath_cflp`, describe capacitated facility location with binary `FacilityOpen[j]` and continuous nonnegative `ShippedAmount[i,j]`. Preserve facility fixed opening costs, facility capacities, customer demands, complete customer-facility transport cost matrix, exact demand satisfaction, ship-only-from-open-facility linking, and open-facility capacity linking. Do not reduce it to pure transportation, omit fixed opening costs, or add vehicle routes/time windows.
- For `optmath_carselection`, describe maximum-cardinality bipartite matching with binary `Assignments[p,c]`. Preserve every participant, every car, the complete participant-car eligibility matrix, assignment-only-if-eligible constraints, participant at-most-one constraints, car at-most-one constraints, and the objective of maximizing the number of assigned eligible pairs. Do not introduce costs, scores, routes, time windows, transportation flows, or required full assignment.
- For `optmath_cell_tower`, describe budgeted maximum coverage with binary `Build[t]` and binary `Covered[r]`. Preserve tower setup costs, region populations, complete tower-region coverage matrix, budget limit, coverage linking, and covered-population maximization. Do not require all regions to be covered and do not reinterpret it as capacitated facility location, shipment planning, or route design.
- For `optmath_revenue_management` and `optmath_revenue`, describe static resource-capacity revenue management with integer package-sales variables. Preserve package revenue, package demand upper bounds, resource capacities, complete package-resource usage matrix, demand-limit constraints, capacity-consumption constraints, and revenue maximization. Do not introduce dynamic pricing, booking periods, stochastic arrivals, nonlinear demand curves, bid-price controls, or cost minimization.
- For `optmath_fleet_routing`, describe a time-expanded fleet-flow model with integer fleet-unit counts, idle fleet counts, initial placement counts, active legs, inactive-leg restrictions, fleet availability, and demand only on listed active legs. Do not use visit-once, subtour, route-arc binary, customer time-window, service-time, or depot-tour language.
- For `optmath_portfolio`, describe continuous portfolio weights, full investment, minimum expected return, weight bounds, and covariance-matrix variance minimization. Do not introduce binary asset selection, cardinality, transaction costs, lots, buy/sell variables, or a maximize-return objective.
- For `optmath_contractallocation`, describe continuous delivery quantities plus binary producer-contract incidence variables. Preserve producer capacities, exact contract fulfillment, minimum contributors, minimum delivery lower bounds, and delivery activation upper bounds. Do not turn it into facility location, routing, award-only selection, or optional fulfillment.
- For `optmath_cut_edited`, describe cutting-stock pattern-count planning with nonnegative integer `Cut[j]` variables. Preserve stock roll width, every order width demand, every listed cutting pattern, exact order fulfillment, and the objective of minimizing total stock rolls. Do not turn it into set cover, bin packing, routing, or binary pattern selection.
- For `optmath_farmplanning`, describe a continuous farm resource LP with planted area, crop sales, consumption-bundle fractions, family/permanent/temporary labor, monthly land/labor/water limits, annual water availability, crop-yield split, and bundle-fraction normalization. Do not add binary crop selection, crop rotation, inventory, or fixed setup costs.
- For `optmath_multisetcover`, describe binary set multicover. Preserve each candidate set cost, each element's required coverage count, the complete set-element coverage matrix, and lower-bound coverage requirements. Do not downgrade multicover to ordinary one-cover set cover or exact coverage.
- For `optmath_mcnd`, describe multicommodity capacitated network design with continuous commodity-flow fractions `x[k,i,j]` and nonnegative integer arc facility counts `y[i,j]`. Preserve commodity origins/destinations/demands, declared arcs, arc capacities, fixed facility costs, commodity-arc routing costs, flow conservation, and capacity linking. Do not make `y[i,j]` binary, collapse commodities, or add vehicle-routing/time-window/inventory structure.
- For `optmath_supplychain`, describe fixed-charge sparse directed network flow with binary arc activation and continuous arc flow only on declared arcs. Preserve node supply/demand balances, fixed arc activation costs, unit flow costs, capacity-activation linking, and the inflow-minus-outflow balance sign convention. Do not replace arc activation with node facility opening or imply a complete graph.
- For `optmath_transp`, describe a balanced transportation LP with continuous nonnegative `Transport[i,j]` shipments. Preserve exact origin supply equalities, exact destination demand equalities, complete origin-destination lane cost matrix, and total-cost minimization. Do not add vehicles, routes, subtours, time windows, lane capacities, fixed charges, binary lane activation, facility opening, unmet-demand slack, or optional unused supply. Do not reveal any internally generated feasible shipment.
- For `optmath_nltrans`, describe capacitated balanced transportation with continuous nonnegative shipments. Preserve exact origin supply equality, exact destination demand equality, lane capacities, and lane cost matrix. Do not reveal any internally generated feasible shipment or add vehicles/routes/time windows.
- For `optmath_multi`, describe balanced multi-commodity transportation with continuous `Transport[i,j,p]`, exact origin-product supply, exact destination-product demand, product-specific shipping costs, and shared origin-destination lane capacity. Do not collapse products or add binary lane activation/fixed costs.
- For `optmath_clsp_expand_capacity`, describe capacitated lot sizing without backlog. Use `compact_lotsizing_tables` as the authoritative data source: include period demand, cumulative demand, setup cost, unit production cost, holding cost, setup big-M, product capacity consumption, and fixed period capacity exactly. Do not introduce backlog, lost sales, capacity-expansion variables, expansion costs, overtime purchases, or a zero-final-inventory hard constraint. Despite the generator name, the source model has fixed capacity, not expansion decisions.
- For `optmath_uncapacitatedlotsizingbacklogging`, describe uncapacitated lot sizing with carried backlog. Use `compact_lotsizing_tables.period_cost_table` exactly; demand is period-by-period demand, not cumulative demand. Preserve total-demand big-M, zero initial inventory/backlog constants, zero final inventory/backlog constraints, and the net-inventory balance `inventory - backlog`. Do not add capacity, lost sales, or independent one-period shortage slack.
- For `optmath_net1`, describe continuous capacitated minimum-cost network flow. Use `compact_network_flow_tables` or `compact_net1_tables` exactly: list every city, every directed arc, each node's separate supply and demand, each arc's unit flow cost, and each arc capacity. Preserve the balance convention `supply + inbound = demand + outbound`; do not aggregate several demand nodes into one sink and do not introduce binary activation, fixed charges, vehicles, routes, time windows, or unmet-demand slack.
- For `optmath_netmcol`, describe continuous multi-commodity network flow over declared directed links. Preserve city-product flow balance, product-specific link capacities, joint link capacities, and product-link shipping costs. Do not add binary activation, fixed costs, path selection, or vehicle routing.
- For `optmath_scooter_location`, describe micromobility station selection and demand assignment with binary `SelectedLocation[j]`, binary `Assign[i,j]`, and nonnegative integer `NewScooters[j]`. Preserve demand at each demand point, existing scooters/opening cost/new-scooter cost at each candidate station, complete distance table, station count limit, total new-scooter budget, assignment-to-selected-station linking, station capacity, and new-scooters-only-at-selected-stations linking. Do not describe routes, tours, shortest paths, TSP, time windows, or vehicle sequencing.
- For `optmath_the_shortest_path_problem`, describe a binary shortest-path model on the declared directed graph. Preserve source, sink, all listed directed arcs, arc costs, binary arc-selection variables, and outflow-minus-inflow balance of 1 at the source, -1 at the sink, and 0 at intermediate nodes. Do not add arc capacities, fixed charges, vehicle routes, subtours, time windows, or facility-opening decisions.
- For `optmath_tsp`, describe a single traveling-salesperson tour using binary directed arc variables and MTZ subtour-elimination order variables. Preserve every city, the depot/start city, complete directed distance table for i != j, exactly-one inbound and outbound selected arc for every city, and one single tour returning to the depot. Do not add vehicle capacity, time windows, service times, multiple vehicles, delivery quantities, or convert it into a shortest-path problem.
- For `optmath_aircraftassignment`, describe aircraft type-to-route capacity allocation with nonnegative integer `Allocation[a,r]` variables. Preserve the aircraft-type availability table, route demand table, aircraft-route capacity contribution matrix, aircraft-route operating cost matrix, availability limits, and route demand coverage constraints. Do not turn it into binary one-aircraft matching, tail routing, runway landing sequencing, passenger spill/recapture, or time-expanded fleet balance. Do not mention any internal feasible allocation certificate.
- For `optmath_aircraftlanding`, describe runway landing-time sequencing with continuous `Landing[i]`, binary pairwise `AircraftOrder[i,j]`, and continuous `Early[i]`/`Late[i]` deviation variables. Preserve earliest/target/latest landing times, early and late penalty rates, ordered-pair separation times, exactly-one precedence for each unordered pair, conditional separation constraints, and landing time windows. Do not turn it into aircraft type assignment, route allocation, gate assignment, tail routing, or makespan-only scheduling. Do not present big-M as business data.
- For `optmath_the_p_dispersion_model`, describe p-dispersion/max-min site spacing. Preserve exact selection of p nodes, pairwise distance table, binary node and pair-selection variables, and continuous minimum-distance objective. Do not add fixed opening costs, capacities, assignments, demand, flows, or maxisum total-distance objective.
- For `optmath_team_formulation`, describe team assignment with skill-shortage minimization. Preserve `Assign[p,c]`, `AttainedSkill[c,s]`, `SkillShortage[c,s]`, `MaxSkillShortage`, exactly-one person assignment, at-least-one person per project, attained-skill balance, and max-shortage bounds. Do not replace it with generic compatibility-score maximization.
- For `optmath_the_multi_factory_schedule_problem`, describe fixed-charge monthly factory production with binary `RunDecision[m,f]`, continuous `Production[m,f]`, fixed run cost, unit production cost, min/max production linking, and monthly demand satisfaction. Do not reinterpret it as job sequencing, flow shop, inventory planning, or product-level lot sizing.
- If the source gives a compact structured table, use that table instead of inventing missing coefficients from an LP preview or reference answer.

Verified instance. Large LP/code artifacts may be summarized for prompt safety; use structured fields first and do not infer omitted source data:

```json
{{INSTANCE_JSON}}
```
